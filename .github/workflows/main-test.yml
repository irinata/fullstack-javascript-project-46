name: Test and lint project
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:      
      - uses: actions/checkout@v4
      - name: run linter
        run: make lint
      - name: run tests
        run: make test

